// SPA mode: prerender the shell, no server-side rendering. This produces the
// static index.html that both the website and the Capacitor mobile apps load.
export const prerender = true;
export const ssr = false;
